{
  "name": "extension-dashboard-mono",
  "private": true,
  "workspaces": [
    "firefox",
    "chrome",
    "shared"
  ],
  "scripts": {
    "build:firefox": "npm run build -w firefox",
    "build:chrome": "npm run build -w chrome",
    "lint:firefox": "npm run lint -w firefox",
    "lint:chrome": "npm run lint -w chrome"
  }
}
